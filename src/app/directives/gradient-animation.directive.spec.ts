import { GradientAnimationDirective } from './gradient-animation.directive';

describe('GradientAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
